@model IEnumerable<WHouse.Models.JobWorker>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.posision)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Userr.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderJob.place)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.posision)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Userr.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderJob.place)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_JobWorker }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_JobWorker }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_JobWorker })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
